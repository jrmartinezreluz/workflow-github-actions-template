# Reusable workflows

Callers must pass `gitops_owner` and `gitops_repository`. Pin the reusable workflow to a tag or SHA.

```yaml
uses: example-org/workflow-github-actions-template/.github/workflows/container-ci.yml@v0.1.0
```
